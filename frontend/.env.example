# Usually leave this unset. Vite proxies /api and /health to localhost:8000 in
# development, while Vercel serves both services from one origin.
# Set it only when intentionally running the backend on another origin.
# VITE_API_URL=http://localhost:8000
