[pytest]
markers =
    slow: marks tests as slow (deselect with -m "not slow")
    requires_clip: marks tests that require torch+transformers and a loadable CLIP model (deselect with -m "not requires_clip")
    requires_visual_model: runs the mandatory SigLIP model against real and generated visual controls
