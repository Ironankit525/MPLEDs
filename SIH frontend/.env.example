# API Connection
VITE_API_BASE_URL=http://localhost:5000/api

# Feature Flags
# By default, the app will try to hit the real backend at VITE_API_BASE_URL.
# Set this to "true" to bypass the backend and use local mock data instead.
VITE_USE_MOCK_DATA=false
