# MPLADS Image Module
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg

# Virtual environment
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Data files (keep structure, ignore contents)
data/images/*.jpg
data/images/*.jpeg
data/images/*.png
data/images/*.webp
data/mplads.db
data/fraud_cases/

# Real photos are user-supplied for local calibration — never commit them
# (keep the README/.gitkeep/directory structure so the instructions ship)
data/real_images/*.jpg
data/real_images/*.jpeg
data/real_images/*.png
data/real_images/*.webp
data/real_images/pairs/*.jpg
data/real_images/pairs/*.jpeg
data/real_images/pairs/*.png
data/real_images/pairs/*.webp

# Evaluation harness output — regenerated by every run, not source
evaluation_report.json
evaluation_report_*.json
evaluation_history.jsonl

# Environment
.env
*.env

# Model cache
*.pt
*.bin
*.safetensors

# Round-3 setup venv (if a teammate uses setup.sh's default .venv name,
# it's already covered above — this covers the pinned-version test venv
# suggested in pyproject.toml's requires-python note)
.venv*/

# Frontend
node_modules/
dist/

# Env
.env
.env.*
!.env.example
!frontend/.env.example

# Agents
.agents/


# Report exports — generated from the live DB by scripts/export_submissions_csv.py
data/exports/
